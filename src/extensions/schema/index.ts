/**
 * Schema extensions for Astral SDK
 *
 * This module provides schema extensions for EAS attestations.
 * Schema extensions allow the SDK to work with different schema formats
 * and customize how data is encoded and decoded for attestations.
 */

// Export built-in schema extensions
export * from './builtins/LocationSchema';

// Export helper functions
export * from './helpers';
